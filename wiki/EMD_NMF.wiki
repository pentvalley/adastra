Idea:

Using time-frequency domain technique empirical mode decomposition (EMD) in combination of nonnegative matrix factorization (NMF) to perform model reduction and extract hidden features.

Then the above processing is forward to a multi-class classifier such as: ANN or SVM(there are such). 